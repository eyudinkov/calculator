# Flutter Calculator

```
- Clone repository:
$ git clone https://github.com/eyudinkov/calculator

- Enter in directory:
$ cd calculator

- For install dependencies:
$ flutter packages get

- Run the app:
$ flutter run
```
